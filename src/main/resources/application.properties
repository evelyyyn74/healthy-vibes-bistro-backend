spring.application.name=hvb-backend
# Conexion al PostgreSQL
spring.datasource.url=jdbc:postgresql://db.nalfdzbjlnuuzinuwvzo.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=Estadias123#
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Servidor
server.port=8080

# JWT ? deja la clave larga (mínimo 32 caracteres) o truena
jwt.secret=clave-secreta-muy-larga-para-hvb-loyalty-2026-cambiala-porfa
jwt.expiration=86400000

spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.initialization-fail-timeout=-1